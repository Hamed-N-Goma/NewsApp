import 'package:flutter/material.dart';

class ScinceScreen extends StatelessWidget {
  const ScinceScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("Scince"));
  }
}
