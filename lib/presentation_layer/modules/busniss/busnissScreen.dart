import 'package:flutter/material.dart';

class busnissScreen extends StatelessWidget {
  const busnissScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Center(child: Text("busniss"));
  }
}
